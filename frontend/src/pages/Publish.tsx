import { Editor } from "../components/Editor";
import { AppBar } from "../components/Appbar";
export const Publish = () => {
    return (
        <div>
            <AppBar />
            <Editor />
        </div>
    );
}